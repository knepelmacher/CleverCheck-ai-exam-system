# שינויי קוד — 2026-07-05

## סבב 2 (תיקון ארכיטקטורה)

השינויים להלן מבצעים:
- העברת לוגיקת computedStatus מה-controller ל-service
- מיפוי ישיר StudentExam.status → computedStatus (ללא ערבוב Exam זמן)
- Job **ללא שינוי**

### שינוי 8: `server/services/exam_service.py` — מימוש מלא

- `get_exams_with_status(student_id)` — שולף repo, מחשב computedStatus, מחזיר list of dicts
- `_STUDENT_EXAM_STATUS_MAP` — מיפוי קבוע: NotStarted→Active, InProgress→InProgress, Submitted→Completed, Checked→Completed
- StudentExam קיים → מיפוי ישיר. אין StudentExam → Exam.status + חלון זמן
- **אפס בדיקות זמן Exam בתוך ענף StudentExam**

### שינוי 9: `server/controllers/exams_controller.py` — ניקוי

- `_compute_exam_status_for_student` ← **נמחקה לחלוטין**
- `get_exams()` ← קוראת ל-`service.get_exams_with_status()`, ממפה ל-JSON
- `get_student_exam(exam_id)` ← הוחלפה ב-`get_exam_by_id(exam_id)` (הפונקציה הישנה קראה למתודות שלא קיימות)
- ייבוא `datetime` ← הוסר
- `subjectID` ← הוחלף ב-`subject` (מחרוזת שם מקצוע)

### שינוי 10: `server/repositories/exam_repository.py` — Subject join

- `get_all_for_student()` ← נוסף outerjoin גם ל-Subject (כדי לקבל subject_name)

### שינוי 11: `student/src/services/examService.ts` — התאמה

- `RawExamItem.subjectID` ← `RawExamItem.subject`
- `exam.subjectID` ← `exam.subject` במיפוי

### Job: `server/jobs/exams_jobs.py` — **ללא שינוי**

### אימות עקביות

| SE.status | Exam | computedStatus | Job |
|---|---|---|---|
| NotStarted | Active (חלון) | Active | — |
| InProgress | any | InProgress | → Submitted |
| Submitted | any | Completed | — |
| Checked | any | Completed | — |
| (none) | Active (חלון) | Active | — |
| (none) | Active (טרם) | Upcoming | — |
| (none) | Closed | Closed | — |

---

---

## שינוי 1: `server/repositories/exam_repository.py`

**תיאור:** הוספת פונקציה `get_all_for_student()` שעושה outer join בין `Exam` ל-`StudentExam` לפי `student_id`. ייבוא של `StudentExam` ו-`and_` נוסף בראש הקובץ.

**למה:** כדי ששליפת כל המבחנים תכלול גם את נתוני `StudentExam` של הסטודנט הספציפי (אם קיים), לצורך חישוב `computedStatus`.

**לפני:** רק `get_all()` שהחזיר `Exam` בלי join.

**אחרי:** נוספה `get_all_for_student(student_id)`.

**שחזור:** למחוק את הפונקציה `get_all_for_student` ואת הייבואים שנוספו.

---

## שינוי 2: `server/services/exam_service.py`

**תיאור:** הוספת מתודה `get_exams_for_student(student_id)`.

**למה:** לעטוף את הקריאה ל-repository החדש.

**לפני:** רק `get_all_exams()`.

**אחרי:** נוספה `get_exams_for_student()`.

**שחזור:** למחוק את המתודה.

---

## שינוי 3: `server/controllers/exams_controller.py`

**תיאור:** 
1. הפונקציה `get_exams()` שונתה לחלוטין: מוסיפה אימות JWT דרך `get_student_data()`, קוראת ל-`get_exams_for_student()`, ומחשבת `computedStatus` ו-`studentExamStatus` לכל מבחן.
2. הפונקציה `get_student_exam(exam_id)` הוחלפה ב-`get_exam_by_id(exam_id)` פשוטה (הפונקציה המקורית קראה למתודות שלא קיימות).
3. נוספה פונקציית עזר `_compute_exam_status_for_student()`.

**למה:** 
- להבטיח שרק סטודנט מאומת רואה מבחנים
- להחזיר סטטוס מחושב לכל מבחן לפי StudentExam.status, Exam.status, וחלון זמן
- למנוע באג מהפונקציה הישנה שקוראת למתודות לא קיימות

**שחזור:** `git checkout HEAD -- server/controllers/exams_controller.py`

---

## שינוי 4: `student/src/types/index.ts`

**תיאור:** 
- `ExamStatus` שונה מ-`'Draft' | 'Active' | 'Closed'` ל-`'Active' | 'InProgress' | 'Completed' | 'Upcoming' | 'Closed'`
- `ExamCardModel` קיבל שדה חדש `studentExamStatus: string | null`

**למה:** להתאים את הטיפוסים לסטטוסים החדשים שהשרת מחזיר.

**שחזור:** `git checkout HEAD -- student/src/types/index.ts`

---

## שינוי 5: `student/src/services/examService.ts`

**תיאור:**
- `normalizeExamStatus()` הוחלף ב-`validateExamStatus()` — פונקציית ולידציה פשוטה (השרת כבר מחשב `computedStatus`)
- `listExams()` עודכן למפות `computedStatus` ל-`status` ו-`studentExamStatus` מהתגובה
- נוסף `interface RawExamItem` לתיאור צורת הנתונים מהשרת

**למה:** הקליינט לא צריך לנחש סטטוסים יותר — השרת הוא מקור האמת.

**שחזור:** `git checkout HEAD -- student/src/services/examService.ts`

---

## שינוי 6: `student/src/pages/DashboardPage.tsx`

**תיאור:**
- `getStatusMeta()`: הוחלפו `'Submitted'`/`'Draft'` ב-`'Completed'`/`'Upcoming'`
- `getActionState()`: פושט — `'Completed'` → צפייה בתוצאות (מופעל), `'Upcoming'`/`'Closed'` → מושבת
- סינון קבוצות: `activeExams` = Active|InProgress, `upcomingExams` = Upcoming, `completedExams` = Completed

**למה:** להתאים לסטטוסים החדשים.

**שחזור:** `git checkout HEAD -- student/src/pages/DashboardPage.tsx`

---

## שינוי 7: `student/src/components/dashboard/ExamCard.tsx`

**תיאור:**
- `statusLabel()`: עדכון תוויות לעברית ומיפוי כל הסטטוסים החדשים
- `actionText`: `Completed` → צפייה בתוצאות, `InProgress` → המשך מבחן, אחרת → התחל מבחן
- `destination`: `Completed` → `/results/:id`, אחרת → `/exam/:id`
- תוויות UI עודכנו לעברית (מקצוע, סטטוס, משך)

**למה:** התאמה לסטטוסים החדשים + תמיכה מלאה בעברית.

**שחזור:** `git checkout HEAD -- student/src/components/dashboard/ExamCard.tsx`
